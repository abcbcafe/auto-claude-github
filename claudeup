#!/bin/bash
# ClaudeUp - Shell wrapper for easy execution

# Get the directory where this script is located
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Execute the Python script
python3 "$SCRIPT_DIR/claudeup.py" "$@"
